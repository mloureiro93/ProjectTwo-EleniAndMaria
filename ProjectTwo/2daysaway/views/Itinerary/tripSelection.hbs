{{!-- 

app.get('/tripSelection', (request, response) => {
response.render('Itinerary/tripSelection');
});

app.get('Itinerary/tripSelection', (request, response) => {
const poisId = Math.floor(Math.random() * 2),

response.render('Itinerary/day1', randomDestination);
}); --}}